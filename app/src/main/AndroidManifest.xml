<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecobit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.registro2Activity"></activity>
        <activity android:name=".Activities.registro1Activity" />
        <activity android:name=".Activities.intro3Activity" />
        <activity android:name=".Activities.intro2Activity" />
        <activity android:name=".Activities.intro1Activity" />
        <activity android:name=".intro1Activity" />
        <activity android:name=".Activities.RegistroActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>